import React from "react";
import { ScrollView, Text } from "react-native";

const inPagos = () => {
    return(
        <ScrollView>

            <Text>
                pagos inquilino
            </Text>

        </ScrollView>
    )
}

export default inPagos