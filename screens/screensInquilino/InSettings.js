import React from "react";
import { ScrollView, Text } from "react-native";

const inSettings = () => {
    return(
        <ScrollView>

            <Text>
                inSettings
            
            </Text>

        </ScrollView>
    )
}

export default inSettings